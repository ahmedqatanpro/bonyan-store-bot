# دليل تشغيل ماركت بنيان للبرمجيات

هذه الحزمة تحتوي على بوت Telegram تجريبي متصل بواجهة VenteBot Reseller API.

## ما تم تجهيزه

يبدأ البوت برسالة ترحيب باسم «متجر بنيان للبرمجيات | Bonyan Software»، ويعرض رقم حساب العميل واسم المستخدم. توجد أزرار للخدمات، المحفظة، طرق الشحن اليدوي، الطلبات، الدعم الفني، وتبديل اللغة بين العربية والإنجليزية.

يتم استيراد الخدمات من API المزود. لكل خدمة يتم حفظ تكلفة المزود، ويُحسب سعر البيع الافتراضي بزيادة 30%. إذا كانت تكلفة الخدمة 1.00 دولار، يكون سعر البيع الافتراضي 1.30 دولار. يمكن للمشرف تغيير السعر من الأمر `/admin_price product_id sale_price_usd`.

زر الدعم ينقل العميل إلى `@BYN_SW`. تظهر وسائل الشحن اليدوي التي حددها صاحب المشروع، وبعد التحويل يرسل العميل الإثبات والمبلغ ورقم حسابه إلى الدعم. بعد التحقق، ينفذ المشرف `/admin_credit telegram_user_id amount_usd`.

## إعداد التشغيل

انسخ `.env.example` إلى `.env`، ثم املأ:

```env
TELEGRAM_BOT_TOKEN=توكن_البوت_الجديد
VENTE_RESELLER_KEY=مفتاح_API_الجديد
ADMIN_IDS=8351443058
```

لا تضع الأسرار في Git أو داخل ZIP أو في رسائل العملاء. التوكن الذي أُرسل في المحادثة مكشوف ويجب إلغاؤه من BotFather وإنشاء توكن جديد.

## التشغيل المحلي

```bash
python3 -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python -m app.main
```

## أهم أوامر المشرف

```text
/admin
/admin_credit telegram_user_id amount_usd
/admin_price product_id sale_price_usd
```

## حالة الدفع

الشحن اليدوي مفعّل كمسار اختبار. الشحن الإلكتروني موجود كهيكل اختياري لكنه لا يعمل دون مزود دفع ورمز صحيح. لا تجعل `ENABLE_WALLET_PURCHASE=true` قبل اختبار الدفع والخصم والاسترداد على خدمة منخفضة القيمة.

## النشر

هذه الحزمة جاهزة للنشر لاحقًا على Render أو خدمة أخرى. الاستضافة المجانية مناسبة للاختبار وقد تتوقف أو تنام، لذلك لا تعتبر مناسبة وحدها لمتجر تجاري 24/7. عند الانتقال للإنتاج نحتاج قاعدة بيانات دائمة، نسخًا احتياطية، HTTPS أو webhook، وعاملًا لمتابعة الطلبات قيد المعالجة.
